{% extends 'layout.njk' %}

{% block content %}
    <main>
            <div class="row box-image-area">
                
                {% for recipe in recipes %}   
            
                <div class="col-4 card" id="{{ recipe.image }}">
                    <img src="{{ recipe.image }}" class="card-img-top" alt="{{ recipe.title }}">
                    <div class="card-body">
                        <p class="text h6" id="{{ recipe.title }}">{{ recipe.title }}</p>
                        <p class="text small" id="{{ recipe.author }}">por {{ recipe.author }}</p>
                    </div>
                </div>

                {% endfor %}

                     {% for recipe in recipes %}   
            
                <div class="col-4 card" id="{{ recipe.image }}">
                    <img src="{{ recipe.image }}" class="card-img-top" alt="{{ recipe.title }}">
                    <div class="card-body">
                        <p class="text h6" id="{{ recipe.title }}">{{ recipe.title }}</p>
                        <p class="text small" id="{{ recipe.author }}">por {{ recipe.author }}</p>
                    </div>
                </div>

                {% endfor %}
                

            </div>
        
    </main>
       <script src="/script.js"></script> 
       
    {% endblock %}


    
    
